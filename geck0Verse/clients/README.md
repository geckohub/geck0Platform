# Remote clients

Copy this folder to Mac, TG or another trusted device and run `./install-remote-client.sh`. Configure `GECK0_JE_HOST`, `GECK0_JE_USER` and `GECK0_JE_PORT` when JE is reached through Tailscale or another address. The launcher calls JE over SSH and avoids reliance on the non-interactive remote PATH.
