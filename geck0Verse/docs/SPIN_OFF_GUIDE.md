# Spin-off guide

Every domain has `project.yaml`, independent service code, data directory and API boundary. Copy the service, matching metadata, required shared modules and Compose service. This keeps each project marketable as an independent tool without requiring the full ecosystem.
