# Crumble + Knowledge Graph

Crumble is the shared command bus for devices, apps, files, schedules, observations, and personal context. The v2.1 foundation uses SQLite tables for nodes, edges, observations, commands, and files. Later migration target: PostgreSQL + pgvector + Neo4j/AGE.
