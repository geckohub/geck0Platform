# v2.2 UX/navigation fix
- Every domain supports: domain, domain dash, domain menu, domain status, domain refresh, domain doctor, domain search.
- Dashboards start in background and print LAN URL.
- `geck0 dash` no longer blocks terminal.
- `.env.example` extended with API placeholders.
