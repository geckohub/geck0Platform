# Cleanup and Polish

Use `geck0 polish` to verify installed commands and directories.
Use `geck0 clean` to safely archive loose home-level scripts/logs.
Use `geck0 integrations` to see missing API keys.
